spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mebeli
    username: postgres
    password: Dosev562
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.xml
  jpa:
    hibernate:
      ddl-auto: validate